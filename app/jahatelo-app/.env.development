# Variables de entorno para desarrollo
# Expo lee variables que comienzan con EXPO_PUBLIC_

# URL del API backend (Next.js)
# NO hardcodear la IP aquí — apiBaseUrl.js la detecta automáticamente desde
# el hostUri de Expo (mismo host que el servidor de desarrollo).
# Funciona desde cualquier red sin tocar este archivo.
# Para forzar una URL específica descomentar la línea siguiente:
# EXPO_PUBLIC_API_URL="http://TU_IP:3000"

# Desactivar caché en desarrollo (1 = desactivado, 0 = activado)
EXPO_PUBLIC_DISABLE_CACHE=1
